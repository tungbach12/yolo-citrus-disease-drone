# Model artifacts — commit các file model chính (best.* + yolov8n.pt) để share,
# nhưng bỏ qua các file sinh lại được / trung gian khác trong models/
models/*
!models/*.pt
!models/*.onnx
!models/*.kmodel
# Không commit checkpoint last.pt (sinh lại được từ Kaggle, phình repo)
**/last.pt
**/last.zip
# File .md mô tả version nằm trong models/v*/ vẫn được commit
!models/v*/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.python-version

# Notebook checkpoints
.ipynb_checkpoints/

# Kaggle
kaggle_out/
.freebuff/

# Env / secrets
.env
*.env
*.local

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
scratch/