# Model artifacts — commit các file model chính (best.* + yolov8n.pt) để share,
# nhưng bỏ qua các file sinh lại được / trung gian khác trong models/
models/*
!models/*.pt
!models/*.onnx
!models/*.kmodel
# Backup/checkpoint phình repo, sinh lại được từ Kaggle — không commit
models/last.pt
models/best_old50.pt

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.python-version

# Notebook checkpoints
.ipynb_checkpoints/

# Kaggle
kaggle_out/

# Env / secrets
.env
*.env
*.local

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db