# Sao chép tệp này thành `.env` rồi điền API key của bạn.
# Lấy key tại: https://roboflow.com/settings/api
# LƯU Ý: file `.env` đã bị .gitignore chặn -> KHÔNG bao giờ push lên GitHub.
ROBOFLOW_API_KEY=your_roboflow_api_key_here